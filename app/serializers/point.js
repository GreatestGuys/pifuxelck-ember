import DS from 'ember-data';

export default DS.RESTSerializer.extend({
  //normalize: function(type, hash) {
  //  console.log("Normalizing a point");
  //  hash.id = 1;
  //  return hash;
  //}
});
